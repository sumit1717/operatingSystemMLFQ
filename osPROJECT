#include<iostream>
#include<queue>
#define arr 100

using namespace std;
int arival[arr],burst[arr],prior[arr],respT[arr],pid[arr];

main()
{
    int i=0,n=0,min,sum=0;
    cout<<"Enter number of processes: \n";
    cin>>n;
    cout<<"ProcessId ArrivalTime BurstTime Prirority \n";
    for(i=0;i<n;i++)
    { cin>>pid[i]>>arival[i]>>burst[i]>>prior[i];
      if(arival[i]>lc)
    	lc=arival[i];
        sum+=burst[i];
    	respT[i]=burst[i];
    } cout<<endl;
    }
   
